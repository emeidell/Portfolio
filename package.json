{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Evan Meidells portfolio",
  "main": "dist/index.html",
  "scripts": {
    "build:css": "tailwind build src/style.css -o dist/style.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.3.3"
  }
}
